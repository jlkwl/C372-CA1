<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= title || 'Payment Failed' %></title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body{ background:#f4f6f9; font-family: Arial, Helvetica, sans-serif; }
    .page-wrap{ max-width: 980px; margin: 40px auto; padding: 0 16px; }
    .card-soft{
      background:#fff;
      border-radius: 14px;
      box-shadow: 0 8px 28px rgba(0,0,0,0.08);
      padding: 26px;
    }
    .title{
      font-size: 56px;
      font-weight: 900;
      color:#111827;
      margin: 0 0 6px;
    }
    .sub{
      color:#6b7280;
      font-size: 16px;
      margin-bottom: 18px;
    }
    .error-box{
      border: 1px solid #f1b4b9;
      background:#f8d7da;
      color:#842029;
      border-radius: 10px;
      padding: 14px 16px;
      font-weight: 700;
      margin: 16px 0;
    }
    .meta{
      font-size: 16px;
      margin: 10px 0;
    }
    .meta b{ color:#111827; }
    .btn-back{
      background:#6b7280;
      border:none;
      color:#fff;
      padding:10px 18px;
      border-radius: 10px;
      text-decoration:none;
      display:inline-block;
    }
    .btn-back:hover{ background:#4b5563; color:#fff; }
    .btn-outline{
      border: 1px solid #9ca3af;
      background:#fff;
      color:#111827;
      padding:10px 18px;
      border-radius: 10px;
      text-decoration:none;
      display:inline-block;
    }
    .btn-outline:hover{ background:#f3f4f6; color:#111827; }
  </style>
</head>

<body>
  <%- include('partials/header', { user: user }) %>

  <div class="page-wrap">
    <div class="card-soft">
      <div class="title">Payment Failed</div>
      <div class="sub">NETS QR payment could not be completed.</div>

      <div class="error-box">
        Error: <%= errorMsg || 'Payment failed.' %>
      </div>

      <div class="meta"><b>Response Code:</b> <%= responseCode || 'N.A.' %></div>
      <div class="meta"><b>Total:</b> $<%= (total != null ? total : '0.00') %></div>
      <div class="meta"><b>Instructions:</b> <%= instructions || 'Please try again.' %></div>

      <div class="mt-4 d-flex gap-2">
        <a href="/checkout" class="btn-back">Back to Checkout</a>
        <a href="/cart" class="btn-outline">Back to Cart</a>
      </div>
    </div>
  </div>
</body>
</html>
