<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Help Centre</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #f8f9fa;
            font-family: Arial, Helvetica, sans-serif;
        }

        .help-container {
            max-width: 900px;
            margin: 40px auto;
        }

        .section-card {
            background: white;
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 23px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }

        .faq-question {
            font-weight: 600;
            margin-bottom: 6px;
        }

        .submit-btn {
            background: #005EB8;
            border: none;
            padding: 10px 20px;
            color: white;
            border-radius: 6px;
            font-size: 16px;
        }

        .submit-btn:hover {
            background: #004a94;
        }
    </style>
</head>
<body>

    <!-- Include your header/navbar -->
    <%- include('partials/navbar', { user: user }) %>

    <div class="help-container">

        <!-- PAGE TITLE -->
        <h2 class="mb-4">Help Centre</h2>

        <!-- FAQ Section -->
        <div class="section-card">
            <h4>Frequently Asked Questions</h4>
            <hr>

            <p class="faq-question">• How do I place an order?</p>
            <p>You can browse items in the Shop page, add them to your cart, and proceed to checkout.</p>

            <p class="faq-question mt-3">• My item is out of stock</p>
            <p>If an item is out of stock, you won’t be able to buy it. You may check back later.</p>

            <p class="faq-question mt-3">• How do I contact support?</p>
            <p>You may submit your feedback or issue using the form below.</p>
        </div>

        <!-- CONTACT / FEEDBACK FORM -->
        <div class="section-card">
            <h4>Send Us Feedback</h4>
            <hr>

            <!-- Display success/error messages -->
            <% if (messages && messages.length) { %>
                <div class="alert alert-success">
                    <% messages.forEach(m => { %>
                        <p><%= m %></p>
                    <% }) %>
                </div>
            <% } %>

            <% if (errors && errors.length) { %>
                <div class="alert alert-danger">
                    <% errors.forEach(e => { %>
                        <p><%= e %></p>
                    <% }) %>
                </div>
            <% } %>

            <form action="/help/feedback" method="POST">
                <div class="mb-3">
                    <label class="form-label">Subject</label>
                    <input type="text" name="subject" class="form-control" required>
                </div>

                <div class="mb-3">
                    <label class="form-label">Message</label>
                    <textarea name="message" class="form-control" rows="5" required></textarea>
                </div>

                <button type="submit" class="submit-btn">Submit Feedback</button>
            </form>
        </div>

    </div>

</body>
</html>
